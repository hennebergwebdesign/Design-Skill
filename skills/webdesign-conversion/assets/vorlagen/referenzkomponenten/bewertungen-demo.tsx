// NUR DEMO-WRAPPER, keine vollständige Komponente.
// Die Nutzervorlage lieferte diesen Aufrufcode für `testimonial-v2` aus der 21st.dev-Registry,
// aber nicht deren Quelltext. Er wird hier nicht nachgebaut, siehe die Regel gegen erfundene
// Belege in der Repo-CLAUDE.md. Für Kundenprojekte gilt ohnehin: echte Bewertungen kommen
// über die Google Places API, siehe ../../../../agentur-website-builder/references/google-bewertungen.md,
// nicht als statisches Fließtext-Testimonial. Siehe auch
// ../../../references/23-referenzkomponenten-21st.md, Abschnitt "Bewertungen/Testimonials".

import Component from "@/components/ui/testimonial-v2";

export default function DemoOne() {
  return <Component />;
}
